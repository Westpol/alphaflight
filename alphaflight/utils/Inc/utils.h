#include <stdint.h>

#ifndef UTILS_H_
#define UTILS_H_

uint32_t UTILS_MAX_I(uint32_t var1, uint32_t var2); // returns the highest number of the two
uint32_t UTILS_MIN_I(uint32_t var1, uint32_t var2); // returns the lowest number of the two
float UTILS_MAX_F(float var1, float var2);  // returns the highest number of the two
float UTILS_MIN_F(float var1, float var2);  // returns the lowest number of the two

#endif